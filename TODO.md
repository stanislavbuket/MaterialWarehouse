# TODO

## Етап 1: Інфраструктура бази даних
- [x] **1.1. Підключення до бази даних**: Прописати адресу підключення до локального MS SQL Server у файлі `appsettings.json` (секція `ConnectionStrings:DefaultConnection`).
- [x] **1.2. Створення файлу міграції**: Згенерувати автоматичний сценарій створення таблиць на основі наших C# класів за допомогою команди в консолі:
  `dotnet ef migrations add InitialCreate --project MaterialWarehouse.DAL --startup-project MaterialWarehouse.API`
- [x] **1.3. Створення реальних таблиць**: Застосувати цей сценарій до бази даних, щоб фізично створити таблиці за допомогою команди:
  `dotnet ef database update --project MaterialWarehouse.DAL --startup-project MaterialWarehouse.API`
- [x] **1.4. Стартове наповнення (Seed Data)**: Додати в код ініціалізації бази автоматичне створення кількох стандартних категорій, матеріалів та перших користувачів з різними ролями, щоб система мала дані для роботи при першому запуску.

## Етап 2: Безпека та авторизація користувачів
- [x] **2.1. Хешування паролів**: Налаштувати роботу з паролями за допомогою вбудованого в .NET класу `PasswordHasher<T>` з пакету `Microsoft.AspNetCore.Identity` (без встановлення сторонніх бібліотек).
- [x] **2.2. Ендпоінт реєстрації**: Створити кінцеву точку `POST /api/auth/register`, яка прийматиме дані нового клієнта (`RegisteredUser`) та додаватиме його до бази з безпечно захешованим паролем.
- [x] **2.3. Ендпоінт входу (JWT)**: Створити кінцеву точку `POST /api/auth/login`. При правильному паролі вона повертатиме цифровий JWT-токен із правами користувача.
- [x] **2.4. Захист складських операцій**: Обмежити доступ до складських методів. Додавання, видалення та зміна кількості матеріалів мають бути дозволені лише користувачам із токеном ролі `Admin` або `Manager` (використовуючи атрибути авторизації ASP.NET Core на ендпоінтах).

## Етап 3: Облік матеріалів та постачання
- [x] **3.1. Створення сутності постачальників**: Додати клас `Supplier` (Id, Name, Contacts) у DAL та налаштувати для нього таблицю через Fluent API.
- [x] **3.2. Налаштування аудиту (Історія руху)**: Створити таблицю `StockTransaction` для фіксації будь-якої дії з товаром (прихід, списання браку, продаж) із зазначенням типу операції, кількості, дати та відповідального менеджера.
- [x] **3.3. Ліміт мінімальних запасів**: Додати до властивостей матеріалу поле `MinStockLimit` (мінімальна критична кількість), щоб система могла попереджати про закінчення товару.
- [x] **3.4. Сервіс прийому та списання товарів**: Створити методи `ReceiveStockAsync` (прийом партії від постачальника) та `WriteOffStockAsync` (списання браку) в BLL сервісах із обов'язковим записом відповідної транзакції в історію руху.

## Етап 4: Управління замовленнями та резервування
- [x] **4.1. Додавання поля резерву**: Додати в сутність `Material` поле `ReservedQuantity` (кількість одиниць, які покупці замовили, але ще не вивезли зі складу).
- [x] **4.2. Бронювання при покупці**: Оновити метод створення замовлення `CreateOrderAsync`. Під час покупки вільний товар (`Кількість - Резерв`) має бронюватися (додаватися до `ReservedQuantity`). Якщо товару не вистачає — оформлювати цей пункт як предзамовлення (`IsPreOrder`).
- [x] **4.3. Фізичне відвантаження зі складу**: При зміні статусу замовлення на `Shipped` (Відправлено) зменшувати фізичну кількість товару `Quantity` та зарезервовану кількість `ReservedQuantity`, а також записувати транзакцію витрати.
- [x] **4.4. Транзакційна цілісність**: Забезпечити, щоб оновлення замовлення, зміна балансу складу та збереження історії руху відбувалися в межах однієї транзакції EF Core через `UnitOfWork.SaveChangesAsync`.

## Етап 5: Контроль та аналітичні звіти для керівництва
- [x] **5.1. Звіт про критичні залишки**: Створити ендпоінт `GET /api/reports/low-stock`, який виведе список товарів, чия кількість на складі впала нижче за встановлений мінімальний ліміт.
- [x] **5.2. Звіт о дефіциті**: Створити ендпоінт `GET /api/reports/deficit`, що покаже матеріали, яких не вистачає на складі для покриття вже створених та зарезервованих замовлень клієнтів.

## Етап 6: Автоматична перевірка та тестування безпеки
- [x] **6.1. Модульні тести логіки**:
  - Перевірити резервування: переконатися, що резерв матеріалів збільшується при покупці, а предзамовлення створюється при дефіциті.
  - Перевірити відвантаження: переконатися, що при переході до статусу `Shipped` фізичні залишки зменшуються, а резерв очищується.
- [x] **6.2. Інтеграційні тести безпеки API**:
  - Написати тести з використанням `WebApplicationFactory` для перевірки доступу до ендпоінтів.
  - Перевірити, що анонімний запит або запит клієнта до методів коригування запасів (наприклад, `POST /api/materials/{id}/adjust`) повертає статус `401 Unauthorized` або `403 Forbidden`.
  - Перевірити, що авторизований менеджер успішно отримує доступ до цих методів (`200 OK`).
